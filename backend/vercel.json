{
  "version": 2,
  "functions": {
    "server.js": {
      "maxDuration": 30  // Increase timeout duration to 30 seconds
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.js"
    }
  ]
}
